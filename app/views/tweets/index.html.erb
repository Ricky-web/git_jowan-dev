<article>
  <%= render partial: 'commons/flash', object: flash %>
  <%= paginate @tweets, turbolinks: false %>
  <%= render partial: 'commons/tweet', locals: {tweets: @tweets} %>
  <%= paginate @tweets %>
</article>